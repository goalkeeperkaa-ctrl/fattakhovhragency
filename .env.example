# Primary destination: generic webhook for lead form submissions from /api/lead
LEAD_WEBHOOK_URL="https://example.com/webhook"

# Fallback destination (if LEAD_WEBHOOK_URL is empty): Telegram bot delivery
TG_BOT_TOKEN="123456:ABC"
TG_CHAT_ID="123456789"

# Optional Cloudflare Turnstile spam protection
VITE_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# Optional: enable client-side admin panel
VITE_ADMIN_ENABLED="false"

# Admin API auth token (required for article create/update/delete)
ADMIN_PANEL_TOKEN="change-me"

# Optional GitHub persistence for admin articles (recommended for Vercel)
GITHUB_REPO="owner/repo"
GITHUB_TOKEN="ghp_xxx"
GITHUB_BRANCH="main"

# Public site URL for sitemap/canonical references
APP_URL="https://fattakhovhragency.vercel.app"
